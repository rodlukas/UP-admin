import { style } from "@vanilla-extract/css"

export const courseCircle = style({
    marginRight: "0.25rem",
})

// kotva pro selector v Card.css.ts (globalStyle pro preskrtnuti minulych skupin)
export const plainName = style({})
